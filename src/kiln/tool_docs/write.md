### Write

Writes a file to the filesystem. Overwrites the file if it already exists.

- If the file already exists, you must Read it first. The tool will fail if you haven't.
- Prefer editing existing files over creating new ones.
