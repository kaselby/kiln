"""Kiln services — optional extensions to the daemon.

Services are self-contained capabilities that integrate with the daemon
via its event bus and RPC registration. Each service owns its own state,
handlers, and lifecycle.
"""
